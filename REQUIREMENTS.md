# 견적 검토 앱 요구사항

> 알파카 물류(가상 회사) 부산→하이퐁 운임 문의 검토용.
> 문의·요율·이름은 모두 수업용 가상 데이터다. 실제 발송·예약·견적 확정은 범위 밖이다.

## 1. 사용자

**알파카 물류 고객지원 담당자.**
고객 운임 문의를 받아 조건을 정리하고, 빠진 값을 확인하고, 견적과 회신 초안을 만드는 사람.

줄이려는 일: 문의 메일에서 조건을 손으로 옮겨 적고, 빠진 값을 다시 묻고, 요율표에서 금액을 골라 더하는 시간.

## 2. 입력 → 처리 → 결과

### 입력
| 항목 | 설명 | 예시 |
|---|---|---|
| 문의 원문 | 고객이 보낸 메일 본문 | `data/inquiries.json` 4건 |
| 요율표 | 항목별 단가·통화·단위·적용기간 | `data/rates.json` |
| 출처표 | 각 요율이 어느 공지에서 왔는지 | `data/sources.json` |

담당자가 확인·수정하는 필드:
문의자 / 출발항 / 도착항 / 컨테이너 종류(GP·HC·RF) / 크기(20·40) / 수량 /
컨테이너당 중량 / B/L 건수 / 품명 / 화물 준비일 / 희망 출항일 / 위험물 여부 / 운송 범위

### 처리
1. **정보 추출** — 문의 원문에서 위 필드를 읽어 폼에 채운다. 원문에 없는 값은 비워 둔다(`null`).
2. **사람 확인** ← *사람이 확인하는 자리* — 담당자가 폼의 모든 값을 눈으로 보고 고친다. 앱은 여기를 건너뛰지 않는다.
3. **조건 검사** — 누락값, 지원하지 않는 컨테이너 종류, 적용기간 밖, 날짜 앞뒤 순서, 수량 유효성, 항구 코드, 위험물·운송 범위를 검사한다.
4. **계산** — 조건을 통과한 항목만 코드가 계산한다. 컨테이너당 비용은 수량을, B/L당 비용은 B/L 건수를 곱한다. 통화끼리 더하지 않는다.
5. **회신 초안 작성** — 계산된 금액과 보류 사유로 고객 회신 초안을 만든다.
6. **사람 검토** ← *사람이 확인하는 자리* — 담당자가 초안을 읽고 고친 뒤 직접 보낸다.

### 결과
- **비용 검토 표** — 한 줄마다 항목·출처·공개/추정 표시·적용기간·통화·단위(컨테이너당/B/L당)·단가·수량·금액
- **통화별 소계** — USD / KRW / VND 각각 따로. 일부만 더해졌으면 *부분 소계*로 표시
- **보류 목록** — 계산하지 않은 항목과 그 사유
- **회신 초안** — 담당자가 고쳐서 보낼 수 있는 한국어 문장

## 3. 성공 기준

### ① 정상 (`normal`)
20GP 2대, B/L 1건, KRPUS→VNHPH, 준비 2026-09-22, 출항 2026-09-25, 비위험물, CY-CY

- [ ] 모든 필드가 원문에서 채워진다
- [ ] 통화별 소계가 **USD 1,280 · KRW 340,290 · VND 950,000** 이다
- [ ] 보류 항목이 하나도 없다
- [ ] 수량만 3대로 바꾸면 **USD 1,920 · KRW 490,435 · VND 950,000** 이 된다 (B/L 비용은 그대로)

### ② 수량 누락 (`missing`)
원문에 컨테이너 대수가 없음

- [ ] 수량 칸이 `null`(빈 값)로 남는다. 1이나 0을 지어내지 않는다
- [ ] 컨테이너당 비용이 **계산되지 않고 보류**된다
- [ ] 보류 사유에 "수량 미확인, 사람 확인 필요"가 표시된다
- [ ] 담당자가 수량을 직접 넣으면 그때 계산된다

### ③ HC 요율 없음 (`unsupported`)
40HC 2대 — 요율표에 GP 값만 있음

- [ ] **GP 요율을 HC에 대신 적용하지 않는다**
- [ ] 금액 대신 보류로 표시된다
- [ ] 보류 사유에 "HC 요율 미보유, GP 요율 대체 적용 불가"가 표시된다

### ④ 적용기간 밖 (`expired`)
20GP 2대, 출항 2026-10-08 — 3분기 LSF는 2026-09-30에 끝남

- [ ] LSF(USD 400)가 **적용기간 밖으로 보류**된다
- [ ] 나머지는 정상 계산되어 USD 소계가 **부분 소계 880** 으로 표시된다
- [ ] KRW 340,290 · VND 950,000 은 그대로 나온다
- [ ] 부분 소계임이 화면에 드러난다

## 4. 계산 규칙

- **단위 구분** — `basis: container`는 수량을 곱하고, `basis: bl`은 B/L 건수를 곱한다
- **통화 분리** — USD·KRW·VND는 서로 더하지 않는다. 환산하지 않는다
- **적용기간** — 희망 출항일이 `effectiveFrom` ~ `effectiveTo` 안에 있어야 한다. `effectiveTo`가 `null`이면 종료일 없음
- **출처 표시** — `provenance`가 `public`이면 공개 확인값, `estimate`면 교육용 추정값으로 구분해 표시한다
- **지원 범위** — GP · 비위험물 · CY-CY 만 계산한다. 그 밖은 보류
- **항구 코드** — `KRPUS` / `VNHPH` 정확한 코드 또는 등록된 별칭만 인정한다. 일부 글자 포함은 인정하지 않는다
- **날짜** — 형식뿐 아니라 실제 달력에 있는 날짜인지 검사한다. 준비일이 출항일보다 늦으면 보류
- **수량** — 음수·소수는 보류

## 5. 범위 밖 (하지 않는 것)

- 실제 발송 · 선적 예약 · 예약 확정
- 견적 자동 확정 (담당자 검토 없이 확정하지 않음)
- 고객에게 메일 자동 발송
- 통화 환산 · 환율 적용
- GP 외 컨테이너 종류(HC·RF) 계산
- 요율 데이터 자동 갱신
