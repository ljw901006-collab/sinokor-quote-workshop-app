# 화면 기준 (DESIGN.md)

이 앱의 화면을 고칠 때 먼저 읽는 기준이다. 색과 크기를 그때그때 정하지 않고 여기 값을 쓴다.

## 출처

| 항목 | 내용 |
|---|---|
| 참고 원문 | `references/Clay.reference.md` (수정하지 않고 그대로 보존) |
| 원본 페이지 | <https://getdesign.md/clay/design-md> |
| 작성 주체 | **VoltAgent의 독립 분석**. Clay가 만든 공식 디자인 가이드가 아니다. |
| 라이선스 | MIT (`references/VoltAgent-LICENSE.txt`) |
| 원본 확인일 | 2026-09-21 (`references/Clay.source.json`) |
| 이 문서 작성일 | 2026-09-21 |

> Clay 참고 문서는 남이 정리해 온 분석 메모다. 우리 입력 화면에 맞는 값만 골라 썼다.
> 아래 **골라 쓴 것 / 뺀 것**에 무엇을 왜 뺐는지 적었다.

## 색

| 용도 | 값 |
|---|---|
| 배경 (canvas) | `#fffaf0` — 크림색 |
| 글자 (ink) | `#0a0a0a` |
| 본문 보조 글자 | `#3a3a3a` |
| 흐린 글자 | `#6a6a6a` |
| 카드 배경 | `#faf5e8` |
| 강조 카드 배경 | `#f5f0e0` |
| 실선·경계 | `#e5e5e5` |
| 주요 버튼 배경 | `#0a0a0a` (검정) |
| 주요 버튼 글자 | `#ffffff` |
| 주요 버튼 눌림 | `#1f1f1f` |
| 보조 버튼 배경 | `#fffaf0` / 테두리 `#e5e5e5` / 글자 `#0a0a0a` |
| 성공·통과 | `#22c55e` |
| 주의·보류 | `#f59e0b` |
| 오류 | `#ef4444` |

통화·보류 표시는 위 성공/주의/오류 세 색만 쓴다. 브랜드 색(핑크·라벤더·피치 등)은 쓰지 않는다.

## 글꼴

한국어 **시스템 글꼴**을 쓴다. 웹폰트를 불러오지 않는다.

```css
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "Malgun Gothic",
             "Apple SD Gothic Neo", "Noto Sans KR", system-ui, sans-serif;
```

| 역할 | 크기 | 두께 | 비고 |
|---|---|---|---|
| 제목 (앱 이름) | **36px 이상** | 600 | `letter-spacing: -1px` |
| 섹션 제목 | 24px | 600 | |
| 소제목 | 18px | 600 | |
| **본문** | **16px** | 400 | `line-height: 1.55` |
| **보조 · 라벨 · 도움말** | **14px** | 400 | |
| 표 숫자 | 16px | 500 | 오른쪽 정렬, `tabular-nums` |

Clay의 `Plain Black` 디스플레이 서체는 쓰지 않는다. 한국어 글자가 없어 우리 화면에 맞지 않는다.

## 모서리 (radius)

| 대상 | 값 |
|---|---|
| **버튼 · 입력 칸** | **12px** |
| **카드** | **16px** |
| **강조 카드** | **24px** |
| 작은 배지·표시 | 8px |

알약 모양(`9999px`)은 쓰지 않는다.

## 크기

- **버튼과 입력 칸의 최소 높이는 44px** 이다. 손가락으로 누를 수 있는 크기를 지킨다.
- 버튼 안쪽 여백: `12px 20px`

## 간격 (spacing)

**8 · 16 · 24 · 32px** 네 단계만 쓴다.

| 쓰임 | 값 |
|---|---|
| 라벨과 입력 칸 사이 | 8px |
| 입력 칸끼리, 카드 안쪽 여백 | 16px |
| 카드끼리, 섹션 안쪽 여백 | 24px |
| 섹션끼리 | 32px |

그 사이 값(10px, 18px 등)을 새로 만들지 않는다.

## 골라 쓴 것 / 뺀 것

**골라 쓴 것**
- 크림 배경 `#fffaf0` — 흰 배경보다 오래 봐도 눈이 편하다
- 검정 주요 버튼 `#0a0a0a` — 눌러야 할 곳이 한눈에 보인다
- 둥근 모서리 12px / 16px / 24px — 딱딱하지 않으면서 알약처럼 과하지 않다
- 44px 최소 높이 — 좁은 화면에서도 누르기 쉽다

**뺀 것**
- **컬러 카드** (핫핑크·라벤더·피치·오커) — Clay는 홍보 페이지용으로 쓰지만, 우리는 금액을 읽는 업무 화면이다.
  색은 통과/보류/오류를 구분하는 데만 쓴다.
- **3D 클레이 일러스트 · 풀블리드 히어로** — 업무 화면에 필요 없다
- **72px 디스플레이 서체** — 한국어 글꼴이 없고 화면이 좁아진다
- **쇼케이스의 교시별 색과 탭** — 교안 사이트의 요소지 학습자 앱의 필수 요소가 아니다. 추가하지 않는다

## 지킬 것

- 화면을 고치기 전에 이 파일을 읽는다.
- 여기 없는 색·크기가 필요하면 먼저 이 파일에 적고 나서 쓴다.
- 390px · 768px 폭에서 가로 스크롤이 생기지 않아야 한다.
