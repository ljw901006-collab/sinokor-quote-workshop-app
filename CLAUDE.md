# 이 프로젝트의 약속

알파카 물류(가상 회사) 견적 검토 앱. 작업을 시작할 때 이 파일을 먼저 읽는다.

## 데이터

- **가상 문의만 사용한다.** 실제 고객 정보·실제 선사 견적을 넣지 않는다.
- 값은 세 가지로 구분해 표시한다.
  - **공개 확인값** (`provenance: public`) — 공개 공지에서 확인한 값. 출처와 확인일을 함께 보여준다.
  - **교육용 추정값** (`provenance: estimate`) — 수업용으로 정한 가정. 실제 시세 추정이 아니라고 밝힌다.
  - **미확인값** — 근거가 없는 값. 금액으로 쓰지 않고 보류한다.
- 출처는 `data/sources.json`에 있다. 추정값을 공개 확인값처럼 보이게 쓰지 않는다.

## 빈 값 처리

- **원문에 없는 수량은 `null`로 둔다.** 1이나 0으로 채우지 않는다.
- 빈 값은 사람이 확인해서 넣는다. 앱이 추측해서 채우지 않는다.
- 사용자가 비운 칸은 비운 채로 유지한다. 포커스가 떠나도 기본값을 넣지 않는다.

## 요율 적용

- **GP 요율을 HC나 RF에 대신 쓰지 않는다.** 요율표에 그 종류가 없으면 보류한다.
- 적용기간(`effectiveFrom` ~ `effectiveTo`) 밖이면 계산하지 않고 보류한다.
  `effectiveTo`가 `null`이면 종료일이 없다는 뜻이다.
- 지원 범위는 **GP · 비위험물 · CY-CY** 뿐이다. 그 밖은 보류한다.
- 항구는 정확한 코드(`KRPUS` · `VNHPH`) 또는 등록된 별칭만 인정한다.
  일부 글자가 포함됐다는 이유로 맞다고 보지 않는다.

## 계산

- **컨테이너당 비용**(`basis: container`)은 수량을, **B/L당 비용**(`basis: bl`)은 B/L 건수를 곱한다. 둘을 섞지 않는다.
- **통화별 소계만 낸다.** USD·KRW·VND를 서로 더하지 않고, 환산하지도 않는다.
- 일부 항목이 보류되어 빠졌으면 그 통화는 **부분 소계**임을 표시한다.
- 금액은 화면에 고정하지 않는다. 항상 원본 단가와 수량·건수로 계산한다.

## AI와 코드의 역할

- **AI는 정보를 읽고 문장을 돕는다.** 문의 원문에서 필드를 뽑고, 회신 초안 문장을 쓴다.
- **계산은 코드가 맡는다.** 금액·소계·보류 판정은 AI가 하지 않는다. AI가 낸 숫자를 금액으로 쓰지 않는다.
- 원문이나 입력이 바뀐 뒤 도착한 이전 AI 응답은 적용하지 않는다.

## 키 보관

- **운영자 API 키는 서버 환경변수에만 둔다.** (`GEMINI_API_KEY`)
- 키를 코드·프론트엔드·저장소·로그·URL에 남기지 않는다. `.env`는 커밋하지 않고 `.env.example`은 빈 값으로 둔다.
- **개인 키 입력 화면은 이 앱에 없다.** 비트8-1의 학습용 `/api/connect` 경로와 키 입력 칸은 8-2에서 제거했다.
  서버는 클라이언트가 보낸 키를 받으면 거부한다.
- 로컬에서는 `.env`의 `GEMINI_API_KEY`를, 배포 환경에서는 Vercel의 Environment Variables를 읽는다.
  로컬 `.env`는 Vercel로 넘어가지 않으므로 배포 환경에는 따로 넣어야 한다.
- `GEMINI_MODEL`로 모델을 바꿀 수 있다. 비우면 `gemini-3.5-flash-lite`를 쓴다.
- 오류 메시지 원문에 키가 들어 있으면 가린다.
- 사람에게 키를 묻거나, 키 값을 읽어서 출력하지 않는다.

## 범위 밖

- 실제 발송 · 선적 예약 · 예약 확정
- 견적 자동 확정 (사람 검토 없이 확정하지 않음)
- 고객에게 메일 자동 발송
- 통화 환산


## 화면 작업

- **UI를 고치기 전에 `DESIGN.md`를 먼저 읽는다.** 색·글자 크기·모서리·간격은 그 파일 값을 쓴다.
- `DESIGN.md`에 없는 값이 필요하면 먼저 그 파일에 적고 나서 쓴다.
- Clay 참고 원문(`references/Clay.reference.md`)은 수정하지 않고 그대로 둔다.
  VoltAgent의 독립 분석이므로 공식 Clay 디자인 가이드로 표시하지 않는다.

## 문서 동기화

- 이 프로젝트에 `AGENTS.md`가 있으면 `CLAUDE.md`와 **동일한 내용으로 유지**한다.
